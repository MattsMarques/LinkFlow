<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width= , initial-scale=1.0">
    <title>Cadastro</title>
    <%-include("../partials/cabecalho_semmenu.ejs")%>
</head>
<body> 
    <div class="container">
        <div class="d-flex align-itens-center py-5 m-3 ">
            <main class="form-container w-100 m-auto p-4 rounded" style="max-width: 450px; background-color: #F8F9FA;">
                <h2 class="text-center text-primary mb-4">Crie sua conta</h2>
                <form action="/usuarios" method="POST">
                    <input type="hidden" name="_id" value="<%= usuario._id %>">
                   <div class="form-group mb-2">
                        <label class="form-label">Email:</label>
                        <input value="<%= usuario.email %>" required type="text" name="email" class="form-control">
                   </div>
                   <div class="form-group mb-2">
                        <label class="form-label">Nome:</label>
                        <input value="<%= usuario.nome %>" required type="text" name="nome" class="form-control">
                   </div>
                   <div class="form-group mb-3">
                        <label class="form-label">Senha:</label>
                        <input value="<%= usuario.senha %>" required type="text" name="senha" class="form-control">
                   </div>
                   <div class="form-group mb-1">
                       <input type="submit" value="Salvar" class="btn btn-primary">
                   </div>
                </form> 
                <p>Já possui conta? <a href="/usuarios/login">Faça seu login</a></p>
            </main>
        </div>
    </div>
         <%-include("../partials/rodape.ejs")%> 
</body>
</html>