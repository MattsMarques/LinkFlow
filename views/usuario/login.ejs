<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width= , initial-scale=1.0">
    <title>Cadastro</title>
    <%-include("../partials/cabecalho_semmenu.ejs")%>
</head>
<body>
    <div class="container">
        <%if(status == "1"){%>
            <div class="alert alert-success alert-dismissible">
                <button class="btn-close" data-bs-dimiss="alert"></button>
                Email inválido(s)!
            </div>
        <%}%>
        <%if(status == "2"){%>
            <div class="alert alert-success alert-dismissible">
                <button class="btn-close" data-bs-dimiss="alert"></button>
                senha inválido(s)!
            </div>
        <%}%>
        <div class="d-flex align-itens-center py-5 m-3 ">
            <main class="form-container w-100 m-auto p-4 rounded" style="max-width: 450px; background-color: #F8F9FA;">
                <h2 class="text-center text-primary mb-4">Fazer Login</h2>
                <form action="/usuarios/login" method="POST">
                   <div class="form-group mb-2">
                        <label for="email" class="form-label">Email:</label>
                        <input required type="text" id="email" name="email" class="form-control" required placeholder="informe seu email">
                   </div>
                   <div class="form-group mb-3">
                        <label for="senha" class="form-label">Senha:</label>
                        <input required type="text" id="senha" name="senha" class="form-control" required placeholder="informe sua senha">
                   </div>
                   <div class="form-group mb-1">
                       <input type="submit" value="Entrar" class="btn btn-primary">
                   </div>
                </form> 
                <p>Não tem uma conta? <a href="/usuarios/cadastrar">Crie sua conta</a></p>
            </main>
        </div>
    </div>
         <%-include("../partials/rodape.ejs")%> 
</body>
</html>